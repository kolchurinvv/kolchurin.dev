apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: kolchurin-dev

resources:
  - namespace.yaml
  - configmap.yaml
  - deployments.yaml
  - services.yaml
  - ingress.yaml
  - mirotalk-deployment.yaml
  - mirotalk-ingress.yaml

commonLabels:
  app.kubernetes.io/part-of: kolchurin-dev